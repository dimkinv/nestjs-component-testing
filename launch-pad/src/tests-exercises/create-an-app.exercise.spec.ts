describe('TestingModule exercise: create an app', () => {
  it.skip('creates a Nest app from AppModule and exercises the HTTP surface', async () => {
    // Import Test from @nestjs/testing and request from supertest.
    // Import AppModule from ../app.module.
    // Create a TestingModule that imports AppModule.
    // Turn moduleRef into a Nest application with createNestApplication().
    // Remember to await app.init() before sending requests.
    // Send a GET request to /launches and assert the response status.
    // Stretch goal: also assert that the body shape includes a data array.
    // Cleanup: close the app at the end of the test.
  });
});
