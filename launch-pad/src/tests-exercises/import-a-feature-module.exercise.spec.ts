describe('TestingModule exercise: import a feature module', () => {
  it.skip('imports a narrower feature module instead of AppModule', async () => {
    // Import Test from @nestjs/testing.
    // Import FavouritesModule from ../favourites/favourites.module.
    // Import FavouritesService from ../favourites/favourites.service.
    // In this test, import only FavouritesModule with Test.createTestingModule().
    // Compile the module and get the service you care about from moduleRef.
    // Call one public method, such as FavouritesService.getFavorites('user-1').
    // Assert the returned shape without booting the whole application.
  });
});
