describe('TestingModule exercise: override a provider', () => {
  it.skip('overrides Ll2Service while keeping the rest of the slice real', async () => {
    // Import Test from @nestjs/testing.
    // Import AppModule, LaunchesService, and Ll2Service.
    // Create a TestingModule that imports AppModule.
    // Before compile(), override Ll2Service with useValue(...).
    // Provide only the method your test needs, for example getLaunches or getLaunchById.
    // Compile the module and get LaunchesService from moduleRef.
    // Call the public service method and assert that the returned data matches your fake.
    // Focus on the observable result, not on internal implementation details.
  });
});